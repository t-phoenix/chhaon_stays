# Copy to .env for local dev. On Vercel, set REACT_APP_BACKEND_URL in project env vars.

REACT_APP_BACKEND_URL=http://localhost:8000

# Optional — disable periodic health polling in dev
# ENABLE_HEALTH_CHECK=false
